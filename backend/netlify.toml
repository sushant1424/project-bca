[build]
  command = "python -m pip install --upgrade pip && pip install -r backend/requirements-netlify.txt"
  publish = "backend"

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
