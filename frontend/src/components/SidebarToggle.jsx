import React from 'react';

// SidebarToggle component removed - using hamburger menu instead
const SidebarToggle = ({ isCollapsed, onToggle, className = "" }) => {
  return null; // No arrow toggle needed
};

export default SidebarToggle;
